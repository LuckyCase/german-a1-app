# 📚 Руководство по работе с контентом

Это руководство поможет вам добавлять и редактировать учебные материалы бота.

## 📁 Структура данных

Все учебные материалы хранятся в папке `data/A1/` в формате JSON:

```
data/A1/
├── metadata.json      # Метаданные уровня A1
├── vocabulary/        # Словарь (11 категорий)
├── grammar/          # Грамматика (8 тем)
├── phrases/          # 🔜 Запланировано
├── dialogues/        # 🔜 Запланировано
├── exercises/        # 🔜 Запланировано
└── culture/          # 🔜 Запланировано
```

## ✏️ Редактирование словаря

### Добавление слова в существующую категорию

1. Откройте файл категории, например `data/A1/vocabulary/food.json`
2. Найдите массив `words`
3. Добавьте новый объект:

```json
{
  "de": "das Brot",
  "ru": "хлеб",
  "example": "Ich kaufe Brot.",
  "example_ru": "Я покупаю хлеб."
}
```

4. Сохраните файл
5. Перезапустите бота (или вызовите `reload_content()`)

### Создание новой категории

1. Создайте файл `data/A1/vocabulary/новая_категория.json`
2. Используйте структуру:

```json
{
  "id": "new_category",
  "name": "Название категории",
  "name_de": "Kategorie Name",
  "description": "Описание категории",
  "level": "A1",
  "words": [
    {
      "de": "das Wort",
      "ru": "слово",
      "example": "Beispiel.",
      "example_ru": "Пример."
    }
  ]
}
```

3. Обновите `data/A1/metadata.json`, добавив категорию в список

### Формат слова

- `de` - немецкое слово с артиклем (обязательно)
- `ru` - русский перевод (обязательно)
- `example` - пример предложения на немецком (рекомендуется)
- `example_ru` - перевод примера (опционально, но рекомендуется)

## 📝 Редактирование грамматики

### Добавление вопроса в тест

1. Откройте файл теста, например `data/A1/grammar/articles.json`
2. Найдите массив `questions`
3. Добавьте новый вопрос:

```json
{
  "question": "___ Tisch ist groß.",
  "options": ["Der", "Die", "Das"],
  "correct": 0,
  "explanation": "Tisch - мужской род (der Tisch)"
}
```

### Создание нового теста

1. Создайте файл `data/A1/grammar/новая_тема.json`
2. Используйте структуру:

```json
{
  "id": "new_topic",
  "name": "Название темы",
  "name_de": "Thema Name",
  "description": "Описание темы",
  "level": "A1",
  "theory": {
    "title": "Заголовок теории",
    "content": "Текст теории...",
    "examples": ["Пример 1", "Пример 2"],
    "tips": ["Совет 1", "Совет 2"]
  },
  "questions": [
    {
      "question": "Вопрос?",
      "options": ["Вариант 1", "Вариант 2", "Вариант 3"],
      "correct": 0,
      "explanation": "Объяснение ответа"
    }
  ]
}
```

3. Обновите `data/A1/metadata.json`

### Формат вопроса

- `question` - текст вопроса (обязательно)
- `options` - массив вариантов ответов (минимум 2, обычно 3-4)
- `correct` - индекс правильного ответа (0-based)
- `explanation` - объяснение (рекомендуется)

## 🔄 Обновление метаданных

После добавления нового контента обновите `data/A1/metadata.json`:

```json
{
  "content": {
    "vocabulary": {
      "categories": [
        {"id": "new_category", "file": "new_category.json", "name": "Новая категория"}
      ],
      "total_words": 275
    },
    "grammar": {
      "topics": [
        {"id": "new_topic", "file": "new_topic.json", "name": "Новая тема"}
      ],
      "total_questions": 85
    }
  }
}
```

## 🔧 Программное обновление контента

### Перезагрузка кэша

```python
from bot.content_manager import reload_content

# Очистить кэш и перезагрузить все данные
reload_content()
```

### Получение статистики

```python
from bot.content_manager import get_vocabulary_stats, get_grammar_stats

vocab_stats = get_vocabulary_stats()
grammar_stats = get_grammar_stats()
```

## ✅ Проверка данных

После редактирования JSON файлов проверьте:

1. **Валидность JSON:**
   ```bash
   python -m json.tool data/A1/vocabulary/food.json
   ```

2. **Загрузку в боте:**
   - Запустите бота
   - Проверьте логи на ошибки загрузки
   - Протестируйте новую категорию/тест

3. **Структуру:**
   - Все обязательные поля присутствуют
   - ID уникальны
   - Индексы `correct` в пределах массива `options`

## 📋 Рекомендации

### Для словаря:
- ✅ Используйте определённые артикли (der/die/das)
- ✅ Добавляйте примеры для каждого слова
- ✅ Группируйте слова по тематике
- ✅ Проверяйте правильность переводов

### Для грамматики:
- ✅ Добавляйте теорию для каждой темы
- ✅ Объясняйте каждый ответ
- ✅ Используйте разнообразные примеры
- ✅ Начинайте с простых вопросов

### Общие:
- ✅ Сохраняйте единый стиль оформления
- ✅ Используйте UTF-8 кодировку
- ✅ Коммитьте изменения в Git
- ✅ Тестируйте после изменений

## 🐛 Решение проблем

### Контент не загружается
- Проверьте синтаксис JSON (валидность)
- Убедитесь, что файл в правильной папке
- Проверьте логи бота на ошибки

### Категория/тест не отображается
- Проверьте, что ID уникален
- Убедитесь, что файл добавлен в metadata.json
- Перезапустите бота

### Ошибки в коде
- Проверьте, что все поля заполнены
- Убедитесь, что индексы корректны
- Проверьте кодировку файла (должна быть UTF-8)

---

**Готово!** Теперь вы можете легко добавлять и редактировать учебные материалы! 🎉
