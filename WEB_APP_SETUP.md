# Инструкция по настройке Telegram Web App

## 🚀 Что было сделано

Бот теперь поддерживает **Telegram Web App** - веб-интерфейс, который открывается прямо в Telegram!

## 📋 Что нужно настроить

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Настройте переменные окружения

Добавьте в файл `.env`:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
WEB_APP_URL=https://ваш-домен.com
```

**Важно:** `WEB_APP_URL` должен быть **HTTPS** и доступен из интернета!

### 3. Запустите веб-сервер

В **отдельном терминале** запустите:

```bash
python web_server.py
```

Веб-сервер будет работать на `http://localhost:5000` (или порт из переменной `PORT`).

### 4. Запустите бота

В **другом терминале** запустите:

```bash
python -m bot.main
```

## 🌐 Деплой Web App

### Вариант 1: Локальный тест (ngrok)

Для тестирования локально используйте **ngrok**:

1. Установите ngrok: https://ngrok.com/
2. Запустите веб-сервер: `python web_server.py`
3. В другом терминале: `ngrok http 5000`
4. Скопируйте HTTPS URL (например: `https://abc123.ngrok.io`)
5. Добавьте в `.env`: `WEB_APP_URL=https://abc123.ngrok.io`

### Вариант 2: Деплой на хостинг

Рекомендуемые платформы:
- **Render.com** (бесплатный тариф для Web Services)
- **Railway.app** (бесплатные кредиты)
- **Fly.io** (бесплатный тариф)
- **Heroku** (если есть аккаунт)

#### Пример для Render.com:

1. Создайте **Web Service** (не Background Worker!)
2. **Build Command**: `pip install -r requirements.txt`
3. **Start Command**: `python web_server.py`
4. Добавьте переменные:
   - `TELEGRAM_BOT_TOKEN`
   - `WEB_APP_URL` = URL вашего Render сервиса
5. Скопируйте URL сервиса и обновите `WEB_APP_URL` в `.env` бота

## 🎯 Как это работает

1. Пользователь нажимает кнопку **"🌐 Открыть Web App"** в боте
2. Открывается веб-интерфейс прямо в Telegram
3. Пользователь может:
   - 📚 Учить слова по категориям
   - 📝 Проходить грамматические тесты
   - 📊 Смотреть свой прогресс
4. Все данные сохраняются в базу данных бота

## 🔧 Структура файлов

```
TelegramBot/
├── web_server.py          # Flask веб-сервер для Web App
├── bot/
│   ├── main.py           # Telegram бот
│   ├── handlers/
│   │   └── common.py     # Обновлён для кнопки Web App
│   └── config.py         # Добавлен WEB_APP_URL
└── requirements.txt      # Добавлены Flask зависимости
```

## ⚠️ Важные замечания

1. **HTTPS обязателен** - Telegram требует HTTPS для Web App
2. **Два процесса** - бот и веб-сервер работают отдельно
3. **Порт** - веб-сервер использует порт 5000 по умолчанию (или `PORT` из env)
4. **База данных** - общая для бота и веб-сервера

## 🐛 Решение проблем

### Web App не открывается
- Проверьте, что `WEB_APP_URL` начинается с `https://`
- Убедитесь, что URL доступен из интернета
- Проверьте логи веб-сервера

### Ошибка аутентификации
- Убедитесь, что пользователь открыл Web App через бота (не напрямую)
- Проверьте, что `user_id` передаётся корректно

### Данные не сохраняются
- Проверьте, что база данных доступна
- Проверьте логи веб-сервера на ошибки

## 📱 Тестирование

1. Запустите бота: `python -m bot.main`
2. Запустите веб-сервер: `python web_server.py`
3. Откройте бота в Telegram
4. Нажмите `/start`
5. Нажмите кнопку **"🌐 Открыть Web App"**
6. Должен открыться веб-интерфейс!

---

**Готово!** Теперь ваш бот поддерживает Telegram Web App! 🎉
